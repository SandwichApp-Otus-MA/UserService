rest:
  clients:
    timeout:
      connect-timeout: ${CONNECT_TIMEOUT:5s}
      read-timeout: ${READ_TIMEOUT:30s}
    billing:
      url: ${BILLING_SERVICE_URL:http://localhost:8081}
      endpoints:
        create: ${CREATE_USER_ACCOUNT_ENDPOINT:/v1/user-account/create}